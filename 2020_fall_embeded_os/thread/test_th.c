#include <stdio.h>
#include <pthread.h>

int main(void){
    pthread_t thread1;
    void *thread_function(void *ptr);
    ret = pthread(&thread1, NULL, thread_function, (void*)message1);

    return 0;
}